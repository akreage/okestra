package activity

// pipeline runner
// created by an activity.
// every activity creates a pipeline runner which starts a supervisor.
// for every new task, pipeline runner creates a worker and adds it to its supervisor.

// type PipelineRunner struct {
// 	pipelineRunnerID
// }
